# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
# Set to your frontend URL in production, or * for development
CORS_ORIGIN=*

# Internet Archive Configuration (LEGACY - currently disabled)
# Get credentials from: https://archive.org/developers/tutorial-get-ia-credentials.html
IA_ACCESS_KEY=
IA_SECRET_KEY=
PROXY_URL=

# Arweave Upload Wallet (REQUIRED)
# Path to your Arweave wallet JSON file for uploading to Arweave
ARWEAVE_WALLET_PATH=/path/to/your/arweave-wallet.json

# ArNS Configuration (OPTIONAL - for permanent URLs)
# Path to your ArNS wallet JSON file for setting undernames
# Can be the same wallet as ARWEAVE_WALLET_PATH or a different one
ARNS_WALLET_PATH=/path/to/your/arns-wallet.json
# Your ArNS name (projects will be accessible at {projectID}_{ARNS_NAME}.ar.io)
ARNS_NAME=undertaker
# TTL in seconds (60 = 1 minute, good for testing; 3600 = 1 hour for production)
ARNS_TTL=60

# Scraping Configuration
SCRAPING_MODE=direct

# Security Configuration (hardcoded in application)
# The following security features are automatically enabled:
# - Rate Limiting: 5 requests per IP per 15 minutes on /archive endpoint
# - Concurrent Limit: Maximum 3 simultaneous archive operations
# - Request Timeout: 10 minutes maximum per request
# - Body Size Limit: 10MB maximum request body size
# - Path Sanitization: Prevents directory traversal attacks
# - IP Logging: Client IP addresses logged with each request
